# Provider keys are entered in Settings and stored in the browser by default.
# These optional values are reserved for a private server-managed deployment.
MISTRAL_API_KEY=
OPENAI_API_KEY=
ELEVENLABS_API_KEY=
